
import 'package:flutter/material.dart';
import 'package:modi/theme/colors.dart';

const defaultHorizontalPadding = 40.0;
const defaultVerticalPadding = 10.0;
const defaultScaffoldPadding = EdgeInsets.symmetric(horizontal: defaultHorizontalPadding, vertical: defaultVerticalPadding);
final defaultBorderRadius = BorderRadius.circular(15);
const defaultRadius = Radius.circular(15);
const defaultShadow = BoxShadow(
  color: shadowColor,
  offset: Offset(0, 3),
  blurRadius: 6,
);
